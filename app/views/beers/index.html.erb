<h1>List of beers</h1>
<% if can? :create, Beer %>
  <p><%= link_to 'Add new beer', new_beer_path %></p>
<% end %>
<table class="table table-striped">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Beer</th>
      <th scope="col">Brewery</th>
      <th scope="col">Style</th>
      <th scope="col">Abv</th>
      <th scope="col">Blg</th>
      <th scope="col">IBU</th>
      <th scope="col">Price</th>
    </tr>
  </thead>
  <tbody>
  <% @beers.includes(:brewery, :style).each_with_index do |b, i| %>
    <tr>
      <td><%= i+1 %></th>
      <td><%= b.name %></td>
      <td><%= b.brewery.name %></td>
      <td><%= b.style.name %></td>
      <td><%= b.abv %>%</td>
      <td><%= b.plato %>°</td>
      <td><%= b.ibu %></td>
      <td><%= b.price %></td>
    </tr>
  <% end %>
  </tbody>
</table
